---
name: "system rersource details"
on:
  push:
    branches:
      - main
jobs:
  bash-commands:
    runs-on: ubuntu-latest
  steps:
    - name: "datecommand"
      run: date
    - name: "memory"
      run: free -h
